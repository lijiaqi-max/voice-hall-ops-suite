# 旧项目迁移

旧项目保持原样，不直接修改数据库文件。

1. 成员端覆盖安装后，Room `5 -> 6` 迁移保留候选、互动、话术、素材、任务和历史。
2. 成员登录中台后，在“个人统计”上传旧数据到迁移暂存区，由管理员确认后合并。
3. 厅控端覆盖安装后，Room `1 -> 2` 迁移保留班次、队列、绑定、麦时、指令和待同步事件。
4. 旧 ZIP 备份先校验 `manifest.json` 与文件 SHA-256，再通过管理台暂存；客户按平台账号合并，麦时按原记录 ID 去重。
5. 任何迁移失败都应保留原 APK 数据，不卸载旧应用，不清除应用数据。

